/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { stimulusManagementGetStimulus } from "../../funcs/stimulusManagementGetStimulus.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStimulusRequest$inboundSchema,
};

export const tool$stimulusManagementGetStimulus: ToolDefinition<typeof args> = {
  name: "stimulus-management-get-stimulus",
  description: `Get a stimulus with its complete content

Get a specific Stimulus by identifier from the service provider. Stimuli can be referenced by Assessment Items to display relevant content to the learner.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await stimulusManagementGetStimulus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
