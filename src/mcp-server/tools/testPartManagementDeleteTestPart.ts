/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { testPartManagementDeleteTestPart } from "../../funcs/testPartManagementDeleteTestPart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTestPartRequest$inboundSchema,
};

export const tool$testPartManagementDeleteTestPart: ToolDefinition<
  typeof args
> = {
  name: "test-part-management-delete-test-part",
  description: `Delete a test part and all its sections

Permanently delete a test part from an assessment test. This removes the test part and all its sections (but not the underlying assessment items). The assessment test's XML structure is automatically updated.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await testPartManagementDeleteTestPart(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
