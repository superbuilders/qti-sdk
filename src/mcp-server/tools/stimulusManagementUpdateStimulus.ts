/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { stimulusManagementUpdateStimulus } from "../../funcs/stimulusManagementUpdateStimulus.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateStimulusRequest$inboundSchema,
};

export const tool$stimulusManagementUpdateStimulus: ToolDefinition<
  typeof args
> = {
  name: "stimulus-management-update-stimulus",
  description: `Update a stimulus and its content

Update a Stimulus on the service provider.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await stimulusManagementUpdateStimulus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
