/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assessmentTestManagementCreateAssessmentTest } from "../../funcs/assessmentTestManagementCreateAssessmentTest.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.AssessmentTest$inboundSchema,
};

export const tool$assessmentTestManagementCreateAssessmentTest: ToolDefinition<
  typeof args
> = {
  name: "assessment-test-management-create-assessment-test",
  description: `Create a new assessment test

Create a new assessment test. Supports both JSON and XML formats.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await assessmentTestManagementCreateAssessmentTest(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
