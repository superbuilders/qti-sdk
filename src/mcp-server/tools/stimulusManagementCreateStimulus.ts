/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { stimulusManagementCreateStimulus } from "../../funcs/stimulusManagementCreateStimulus.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.Stimulus$inboundSchema,
};

export const tool$stimulusManagementCreateStimulus: ToolDefinition<
  typeof args
> = {
  name: "stimulus-management-create-stimulus",
  description: `Create a new stimulus

Create a new Stimulus on the service provider. Stimuli can be referenced by Assessment Items.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await stimulusManagementCreateStimulus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
