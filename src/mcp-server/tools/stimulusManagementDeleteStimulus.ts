/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { stimulusManagementDeleteStimulus } from "../../funcs/stimulusManagementDeleteStimulus.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteStimulusRequest$inboundSchema,
};

export const tool$stimulusManagementDeleteStimulus: ToolDefinition<
  typeof args
> = {
  name: "stimulus-management-delete-stimulus",
  description: `Delete a stimulus

Permanently delete a stimulus. This operation cannot be undone. Warning: Assessment items that reference this stimulus may be affected. Consider checking for references before deletion.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await stimulusManagementDeleteStimulus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
