/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assessmentTestManagementDeleteAssessmentTest } from "../../funcs/assessmentTestManagementDeleteAssessmentTest.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteAssessmentTestRequest$inboundSchema,
};

export const tool$assessmentTestManagementDeleteAssessmentTest: ToolDefinition<
  typeof args
> = {
  name: "assessment-test-management-delete-assessment-test",
  description: `Delete an assessment test

Permanently delete an assessment test and all its associated data including test parts, sections, and item references. This operation cannot be undone. The actual assessment items referenced by this test are not deleted.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await assessmentTestManagementDeleteAssessmentTest(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
