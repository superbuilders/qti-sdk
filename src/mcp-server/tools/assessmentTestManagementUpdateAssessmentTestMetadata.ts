/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assessmentTestManagementUpdateAssessmentTestMetadata } from "../../funcs/assessmentTestManagementUpdateAssessmentTestMetadata.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAssessmentTestMetadataRequest$inboundSchema,
};

export const tool$assessmentTestManagementUpdateAssessmentTestMetadata:
  ToolDefinition<typeof args> = {
    name: "assessment-test-management-update-assessment-test-metadata",
    description: `Update assessment test metadata only

Update only the metadata fields (title, description, etc.) of an assessment test without affecting its structure, test parts, sections, or assessment items. This is a lightweight operation for administrative changes.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await assessmentTestManagementUpdateAssessmentTestMetadata(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
