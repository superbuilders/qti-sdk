/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assessmentItemManagementDeleteAssessmentItem } from "../../funcs/assessmentItemManagementDeleteAssessmentItem.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteAssessmentItemRequest$inboundSchema,
};

export const tool$assessmentItemManagementDeleteAssessmentItem: ToolDefinition<
  typeof args
> = {
  name: "assessment-item-management-delete-assessment-item",
  description: `Delete an assessment item

Permanently delete an assessment item. This operation cannot be undone. Warning: Assessment tests that reference this item may be affected. The item references in test sections will need to be updated separately.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await assessmentItemManagementDeleteAssessmentItem(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
