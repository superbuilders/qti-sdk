/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { stimulusManagementSearchStimuli } from "../../funcs/stimulusManagementSearchStimuli.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SearchStimuliRequest$inboundSchema.optional(),
};

export const tool$stimulusManagementSearchStimuli: ToolDefinition<typeof args> =
  {
    name: "stimulus-management-search-stimuli",
    description: `Search and filter stimuli

Get all stimuli on the service provider. Search with advanced filtering capabilities. Supports text search across titles and identifiers, sorting, and pagination.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await stimulusManagementSearchStimuli(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
