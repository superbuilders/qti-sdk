/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { testPartManagementUpdateTestPart } from "../../funcs/testPartManagementUpdateTestPart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTestPartRequest$inboundSchema,
};

export const tool$testPartManagementUpdateTestPart: ToolDefinition<
  typeof args
> = {
  name: "test-part-management-update-test-part",
  description: `Update a test part's configuration and sections

Update a test part including its navigation mode, submission mode, time limits, and sections. This operation updates the entire test part structure and regenerates the parent assessment test's XML.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await testPartManagementUpdateTestPart(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
