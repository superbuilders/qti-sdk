/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { sectionManagementRemoveAssessmentItem } from "../../funcs/sectionManagementRemoveAssessmentItem.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveAssessmentItemRequest$inboundSchema,
};

export const tool$sectionManagementRemoveAssessmentItem: ToolDefinition<
  typeof args
> = {
  name: "section-management-remove-assessment-item",
  description: `Remove an assessment item reference from a section

Remove an assessment item reference from a section. This only removes the reference link; the actual assessment item remains in the assessment-items collection. The assessment test's XML structure is automatically updated.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await sectionManagementRemoveAssessmentItem(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
